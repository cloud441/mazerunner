[tool.poetry]
name = "mazerunner"
version = "0.1.0"
description = ""
authors = ["cloud441"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
